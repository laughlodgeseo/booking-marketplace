[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = [
  "npm install -g --prefix /app/.pnpm pnpm@10.28.2",
  "/app/.pnpm/bin/pnpm install --frozen-lockfile"
]

[phases.build]
cmds = ["/app/.pnpm/bin/pnpm --filter api build"]

[start]
cmd = "/app/.pnpm/bin/pnpm --filter api start:prod"
