[build]
builder = "NIXPACKS"

[deploy]
startCommand = "/app/.pnpm/bin/pnpm --filter api start:prod"
healthcheckPath = "/api/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[services]]
name = "api"
